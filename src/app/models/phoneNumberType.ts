export class PhoneNumberType {
    PhoneNumberTypeId: number;    
    Name: string;
    constructor(phoneNumberTypeId: number, name: string ) {
        this.PhoneNumberTypeId = phoneNumberTypeId;
        this.Name = name;        
  }
}